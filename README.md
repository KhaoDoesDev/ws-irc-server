# ws-irc-server

This project was made as a replacement for Brew Hack's RTC server.
No proper auth, username validation, and everything else a proper chat system should have. (Might be a TODO)

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run start
```